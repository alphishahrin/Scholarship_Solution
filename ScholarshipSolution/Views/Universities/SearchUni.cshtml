@model IEnumerable<ScholarshipSolution.Models.University>

@{
    ViewBag.Title = "SearchUni";
    Layout = "~/Views/Shared/_LayoutProf.cshtml";
}



    <!-- Header -->
    <main>
        <section id="portfolio" class="portfolio">
            <div class="container" data-aos="fade-up">
                <header class="section-header">
                    <h2>Universities</h2>
                    <p>Check our latest work</p>
                </header>
                
                <div>
                    @using (Html.BeginForm("SearchUni", "Universities", FormMethod.Get))
                    {
                        //@Html.TextBox("searching")<input type="submit" value="Search" />
                        @Html.TextBox("searching", null, new { id = "choices-text-preset-values", @class = "form-control", @type = "text", @placeholder = "Type to search..." })<input type="submit" value="Search" />
                    }
                </div>
                <br ><br><br>
                <div class="row gy-4 portfolio-container" data-aos="fade-up" data-aos-delay="200">
                    @if (Model.Count() == 0)
                    {
                        <p style="color:red"> No Match Found!</p>
                    }
                    else
                    {
                        foreach (var item in Model)
                        {
                            <div class="col-lg-4 col-md-6 portfolio-item">
                                <div class="portfolio-wrap">
                                    <img src="@Url.Content(item.UniPicture)" class="img-fluid" width="350" height="350" alt="">
                                    <div class="portfolio-info">
                                        <h4>@Html.DisplayFor(modelItem => item.UniName)</h4>
                                        <p>@Html.DisplayFor(modelItem => item.City), @Html.DisplayFor(modelItem => item.Country)</p>
                                        <span>@Html.DisplayFor(modelItem => item.UniRating)</span>
                                        <div class="portfolio-links">
                                            <a href="@Url.Content(item.UniPicture)" data-gallery="portfolioGallery" class="portfokio-lightbox" title="App 1"><i class="bi bi-plus"></i></a>
                                            <a href="#" title="More Details"><i class="bi bi-link"></i></a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    }
                </div>
            </div>
        </section>
    </main>
